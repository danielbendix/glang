# Synthetic benchmarks

This directory contains python scripts that can be used to create synthetic benchmarks, which can be used to stress test one or more parts of the compiler.
This approach makes it easy to vary the size of a benchmark, and doesn't necessitate adding large files to source control.
