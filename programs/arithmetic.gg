fn iMath(a: i16, b: i16) -> i32 {
    const c = a * b;
    const d = a / b;
    const e = (a + b) % (a - b);
    return e;
}

fn fMath(a: f32, b: f32) -> f64 {
    const c = a * b;
    const d = a / b;
    const e = a + b - c + d;
    return e;
}
